#!/bin/bash
# The shebang tells the kernel which interpreter to use, regardless of the user’s current shell, unless an interpreter is explicitly specified

echo "Hello World"
echo "($ls)"
echo ls
echo "My current working directory is: $(pwd)"
